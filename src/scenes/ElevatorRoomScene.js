import ParkingGarageScene from './ParkingGarageScene.js';

// Walk inside the lobby already illustrated on the garage map. Reusing its
// floor, elevator doors, lighting and brick preserves exact visual continuity.
export default class ElevatorRoomScene extends ParkingGarageScene {
  constructor() { super('ElevatorRoomScene'); }
}
